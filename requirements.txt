# Runtime fallback for environments that install from requirements.txt.
# Keep this list in sync with [project].dependencies in pyproject.toml.
fastapi>=0.115,<1.0
openai>=1.0
requests>=2.32
tenacity>=8.1.0,<9.0.0
langchain-openai>=0.1.0
pydantic>=2.6
pyyaml>=6.0
python-dotenv>=1.0
uvicorn>=0.34,<1.0
